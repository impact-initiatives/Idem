% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/validate_xlsform.R
\name{validate_xlsform}
\alias{validate_xlsform}
\title{Validate an XLSForm against a reference form}
\usage{
validate_xlsform(
  target,
  dev,
  checks = c("question_names", "list_names", "survey_list_names", "choices", "labels")
)
}
\arguments{
\item{target}{An \code{xlsform} object representing the authoritative reference
form.}

\item{dev}{An \code{xlsform} object representing the form being validated.}

\item{checks}{A character vector of check names to run. Defaults to all
five checks:
\verb{c("question_names", "list_names", "survey_list_names", "choices",}
\verb{"labels")}.}
}
\value{
A tibble with columns \code{check}, \code{severity}, \code{name}, \code{list_name}, and
\code{detail}. Has zero rows when no issues are found.
}
\description{
Runs one or more validation checks comparing a \code{dev} (work-in-progress)
XLSForm against a \code{target} (authoritative reference) XLSForm. The default
direction checks that everything present in \code{target} also exists in \code{dev}
— i.e., \code{target} is a valid subset of \code{dev}.
}
\details{
This is the main entry point for form validation. It delegates to the
individual \verb{validate_*()} functions and combines their results into a single
tibble.
\subsection{Available checks}{\tabular{ll}{
   Check name \tab What it tests \cr
   \code{"question_names"} \tab Every question name in \code{target} must exist in \code{dev}. \cr
   \code{"list_names"} \tab Every list name \emph{defined} in \code{target}'s choices sheet must also be defined in \code{dev}'s choices sheet. \cr
   \code{"survey_list_names"} \tab Every list name \emph{referenced} in \code{target}'s survey questions must also be referenced in \code{dev}'s survey questions. \cr
   \code{"choices"} \tab For every shared list, every choice option in \code{target} must exist in the same list in \code{dev}. \cr
   \code{"labels"} \tab Translation columns in \code{target} and \code{dev} are well-formed. \cr
}


The \code{"labels"} check delegates to \code{\link[=check_labels]{check_labels()}} and runs it on both
\code{target} and \code{dev} independently.
}

\subsection{Return value structure}{

Each row in the returned tibble represents one validation issue:\tabular{ll}{
   Column \tab Description \cr
   \code{check} \tab Which check produced this issue. \cr
   \code{severity} \tab Currently always \code{"error"}. \cr
   \code{name} \tab The name of the offending question or choice option. \cr
   \code{list_name} \tab The choices list involved (\code{NA} for question-level checks). \cr
   \code{detail} \tab A human-readable description of the problem. \cr
}

}
}
\examples{
target <- read_xlsform(system.file("extdata/form.xlsx", package = "idem"))

# No issues: a form is always a valid subset of itself
validate_xlsform(target, target)

# Run only a subset of checks
validate_xlsform(target, target, checks = c("question_names", "choices"))

# Introduce issues: dev is missing a question and a choice option that
# target requires
dev_trimmed <- xlsform(
  survey  = target$survey[-nrow(target$survey), ],
  choices = target$choices[-nrow(target$choices), ]
)
issues <- validate_xlsform(target, dev_trimmed)
issues
}
\seealso{
\code{\link[=validate_question_names]{validate_question_names()}}, \code{\link[=validate_list_names]{validate_list_names()}},
\code{\link[=validate_survey_list_names]{validate_survey_list_names()}}, \code{\link[=validate_choices]{validate_choices()}},
\code{\link[=check_labels]{check_labels()}} for the individual checks.
}
