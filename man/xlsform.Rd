% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/xlsform.R
\name{xlsform}
\alias{xlsform}
\title{Construct an xlsform object from data frames}
\usage{
xlsform(..., path = NA_character_)
}
\arguments{
\item{...}{Named data frames, one per sheet. Names become the sheet names
(e.g. \verb{survey =}, \verb{choices =}). All arguments must be named and must be
data frames.}

\item{path}{A string recording the (notional) source path. Defaults to
\code{NA_character_} for in-memory objects.}
}
\value{
An \code{xlsform} object: a named list of data frames with class
\code{c("xlsform", "list")} and a \code{path} attribute.
}
\description{
Builds an \code{xlsform} object directly from in-memory data frames, without
reading from a file. The resulting object is structurally identical to one
produced by \code{\link[=read_xlsform]{read_xlsform()}}, making it useful for testing, creating minimal
reproducible examples, or programmatically assembling forms.
}
\details{
Most Idem functions expect at least a \code{survey} sheet and, for choice-related
operations, a \code{choices} sheet.
}
\examples{
# Minimal form with two select_one questions sharing a yes/no list
survey <- data.frame(
  type = c("select_one yn", "select_one yn", "text"),
  name = c("consent", "satisfied", "comments")
)
choices <- data.frame(
  list_name = c("yn", "yn"),
  name      = c("yes", "no"),
  label     = c("Yes", "No")
)
form <- xlsform(survey = survey, choices = choices)
form

# In-memory forms can be passed directly to validate_xlsform()
validate_xlsform(form, form)
}
\seealso{
\code{\link[=read_xlsform]{read_xlsform()}} to load an \code{xlsform} object from an \code{.xlsx} file.
}
