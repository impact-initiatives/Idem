% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/xlsform_translations.R
\name{xlsform_translations}
\alias{xlsform_translations}
\alias{xlsform_translations.default}
\alias{xlsform_translations.xlsform}
\title{Get declared translations from an XLSForm}
\usage{
xlsform_translations(x, ...)

\method{xlsform_translations}{default}(x, ...)

\method{xlsform_translations}{xlsform}(x, ...)
}
\arguments{
\item{x}{An \code{xlsform} object.}

\item{...}{Ignored; present for S3 method compatibility.}
}
\value{
A tibble with columns:
\describe{
\item{sheet}{The sheet the column was found in (\code{"survey"} or
\code{"choices"}).}
\item{field}{The translatable field prefix (e.g. \code{"label"}, \code{"hint"}).}
\item{language}{The language tag as written after \code{::} (e.g.
\code{"English (en)"}, \code{"French (fr)"}).}
\item{column}{The original (untrimmed) column name.}
}
Has zero rows when no translated columns are found.
}
\description{
Scans the column names of the \code{survey} sheet (and the \code{choices} sheet when
present) for columns matching any \link{translatable_fields} prefix followed by a
\verb{::language} suffix, e.g. \code{label::English (en)} or \code{hint::French (fr)}.
Returns a tidy tibble describing every translation found, one row per
field–language combination.
}
\details{
A valid translated column name consists of a \link{translatable_fields} prefix,
\code{::}, a language name (which may contain non-ASCII characters), a single
space, and a parenthesised IANA language subtag in lowercase — for example
\code{"label::English (en)"} or \code{"hint::Français (fr)"}. Columns that do not
follow this convention (bare \code{"label"}, \code{"label::English"} without a code,
\code{"label::Spanish(es)"} without a space, \code{"label::French (FR)"} with an
uppercase code) are not returned.

Use the \href{https://www.iana.org/assignments/language-subtag-registry}{IANA language subtag registry} to look up
the correct code for a given language.

Column names are trimmed of leading and trailing whitespace before matching,
so \code{" label::English (en) "} is treated identically to
\code{"label::English (en)"}.
}
\examples{
form <- read_xlsform(system.file("extdata/form.xlsx", package = "idem"))
xlsform_translations(form)
}
\seealso{
\link{translatable_fields} for the full list of recognised field
prefixes; \code{\link[=check_labels]{check_labels()}} for validation built on top of this accessor.
}
