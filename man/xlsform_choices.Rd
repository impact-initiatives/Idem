% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/xlsform_choices.R
\name{xlsform_choices}
\alias{xlsform_choices}
\alias{xlsform_choices.default}
\alias{xlsform_choices.xlsform}
\title{Get choice options from an XLSForm}
\usage{
xlsform_choices(x, ...)

\method{xlsform_choices}{default}(x, ...)

\method{xlsform_choices}{xlsform}(x, ...)
}
\arguments{
\item{x}{An \code{xlsform} object.}

\item{...}{Ignored; present for S3 method compatibility.}
}
\value{
A named list of character vectors. Each name is a list name; each
element is the character vector of option \code{name} values for that list. Rows
with \code{NA} in either \code{list_name} or \code{name} are silently dropped.
}
\description{
Returns a named list of character vectors, where each name is a list name and
each element contains the choice option \code{name} values for that list. Both the
\code{choices} sheet and, when present, the \code{external_choices} sheet are combined.
}
\details{
This is useful for inspecting which options are available for a given
\code{select_one} or \code{select_multiple} question, and is used internally by
\code{\link[=validate_choices]{validate_choices()}} to compare option sets across two forms.
}
\examples{
form <- read_xlsform(system.file("extdata/form.xlsx", package = "idem"))

# All choice options, organised by list name
xlsform_choices(form)

# Options for a specific list
xlsform_choices(form)[["yn"]]
}
\seealso{
\code{\link[=xlsform_choices_list_names]{xlsform_choices_list_names()}} for just the list names;
\code{\link[=validate_choices]{validate_choices()}} to compare choice options across two forms.
}
