% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/validate_choices.R
\name{validate_choices}
\alias{validate_choices}
\title{Validate choice options between two XLSForms}
\usage{
validate_choices(target, dev, passing_lists = idem_passing_lists)
}
\arguments{
\item{target}{An \code{xlsform} object representing the authoritative reference
form.}

\item{dev}{An \code{xlsform} object representing the form being validated.}

\item{passing_lists}{A character vector of list names to skip entirely.
Defaults to \link{idem_passing_lists}. Pass \code{character(0)} to disable all
bypasses.}
}
\value{
A tibble with columns \code{check}, \code{severity}, \code{name}, \code{list_name}, and
\code{detail}. Has zero rows when all choice options in \code{target} are present in
\code{dev} for every shared list.
}
\description{
For every list name that exists in \emph{both} \code{target} and \code{dev}'s choices
sheets, checks that each choice option name present in \code{target} also exists
in \code{dev}. Returns a tibble row for each option found in \code{target} that is
absent from \code{dev} for the same list.
}
\details{
\subsection{Scope of this check}{

This check only compares lists that are defined in \emph{both} forms. Lists that
appear in \code{target} but are entirely absent from \code{dev} are not reported here
— use \code{\link[=validate_list_names]{validate_list_names()}} to catch those gaps first.

A typical validation workflow runs \code{\link[=validate_list_names]{validate_list_names()}} before
\code{validate_choices()}, or simply calls \code{\link[=validate_xlsform]{validate_xlsform()}} which runs both.
}
}
\examples{
target <- read_xlsform(system.file("extdata/form.xlsx", package = "idem"))

# No issues: all choice options in target also exist in dev
validate_choices(target, target)

# Issues found: drop one option from a non-passing list
non_passing_row <- which(
  !is.na(target$choices$list_name) &
    !target$choices$list_name \%in\% idem_passing_lists
)[1]
dev_trimmed <- xlsform(
  survey  = target$survey,
  choices = target$choices[-non_passing_row, ]
)
validate_choices(target, dev_trimmed)

# Extend the default passing_lists with a project-specific list
validate_choices(
  target, target,
  passing_lists = c(idem_passing_lists, "l_my_project_list")
)
}
\seealso{
\code{\link[=validate_xlsform]{validate_xlsform()}} to run all checks together;
\code{\link[=validate_list_names]{validate_list_names()}} for checking that lists themselves exist in \code{dev};
\code{\link[=xlsform_choices]{xlsform_choices()}} to extract choice options from a form.
}
