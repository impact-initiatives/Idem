% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/check_labels.R
\name{check_labels}
\alias{check_labels}
\alias{check_labels.default}
\alias{check_labels.xlsform}
\title{Check translation consistency of an XLSForm}
\usage{
check_labels(x, ...)

\method{check_labels}{default}(x, ...)

\method{check_labels}{xlsform}(x, ...)
}
\arguments{
\item{x}{An \code{xlsform} object as returned by \code{\link[=read_xlsform]{read_xlsform()}} or
\code{\link[=xlsform]{xlsform()}}.}

\item{...}{Ignored; present for S3 method compatibility.}
}
\value{
A tibble with columns \code{check}, \code{severity}, \code{name}, \code{list_name},
and \code{detail}. Has zero rows when no issues are found.
}
\description{
Inspects the column names of the \code{survey} sheet (and the \code{choices} sheet for
\code{label}) to enforce that all \link{translatable_fields} are explicitly declared
with a language suffix and that the set of languages is consistent across
fields.
}
\details{
Column matching is done after stripping leading and trailing whitespace from
column names, so \code{" label::English (en) "} is treated the same as
\code{"label::English (en)"}.
\subsection{Checks performed}{

\strong{Bare or malformed column (error)} -- A translatable field is present
either as a plain column with no suffix (e.g. \code{label}, \code{hint}) or as a
malformed translation attempt that does not follow the
\code{field::Language (code)} convention (e.g. \code{label::English} without an ISO
code, \code{label::French (FR)} with an uppercase code, \code{label:Kreyol} with a
single colon). The user must use an explicitly language-tagged column such
as \code{label::English (en)}. Applies to \code{label} in both the \code{survey} and
\code{choices} sheets; applies to all other fields in \code{survey} only.

\strong{Survey-choices label language mismatch (error)} -- The set of languages
declared on \code{label} columns in the \code{survey} sheet must exactly match the
set declared on \code{label} columns in the \code{choices} sheet. Any language
present in one sheet but absent from the other is an error. This check is
skipped when the form has no \code{choices} sheet.

\strong{Non-label field language mismatch (error)} -- A non-\code{label} translatable
field in the \code{survey} sheet (e.g. \code{hint}, \code{constraint_message}) is declared
with a language that was not declared for any \code{label} column in \code{survey}.
The languages detected on survey \code{label} columns form the reference set.

\strong{Missing \code{default_language} (warning)} -- When a form declares more than
one label language, XLSForm best practice requires a \code{default_language}
entry in the \code{settings} sheet so that data collection tools know which
translation to show by default. This warning fires when the \code{settings}
element is absent from the \code{xlsform} object, when it has no
\code{default_language} column, or when \code{default_language} is \code{NA} or empty.
To suppress the warning, load the form with
\code{read_xlsform(path, optional_sheets = "settings")} and ensure the
\code{settings} sheet contains a non-empty \code{default_language} value.
}
}
\examples{
# No issues: the fixture form uses valid translated columns throughout
form <- read_xlsform(
  system.file("extdata/form.xlsx", package = "idem"),
  optional_sheets = "settings"
)
check_labels(form)

# Bare label column (no language suffix) -- error
bare <- xlsform(
  survey = tibble::tibble(
    type  = "text",
    name  = "q1",
    label = "What is your name?"
  )
)
check_labels(bare)

# Malformed translation attempt (missing ISO code) -- error
malformed <- xlsform(
  survey = tibble::tibble(
    type             = "text",
    name             = "q1",
    `label::English` = "What is your name?"
  )
)
check_labels(malformed)

# Language mismatch: hint in Spanish but label only in English -- error
mismatch <- xlsform(
  survey = tibble::tibble(
    type                  = "text",
    name                  = "q1",
    `label::English (en)` = "What is your name?",
    `hint::Spanish (es)`  = "Tu nombre"
  )
)
check_labels(mismatch)
}
\seealso{
\link{translatable_fields} for the full list of field prefixes
inspected; \code{\link[=validate_xlsform]{validate_xlsform()}} to run cross-form checks.
}
