% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/validate_list_names.R
\name{validate_list_names}
\alias{validate_list_names}
\title{Validate defined list names between two XLSForms}
\usage{
validate_list_names(target, dev)
}
\arguments{
\item{target}{An \code{xlsform} object representing the authoritative reference
form.}

\item{dev}{An \code{xlsform} object representing the form being validated.}
}
\value{
A tibble with columns \code{check}, \code{severity}, \code{name}, \code{list_name}, and
\code{detail}. Has zero rows when all list names defined in \code{target}'s choices
are also defined in \code{dev}'s choices.
}
\description{
Checks that every list name \emph{defined} in \code{target}'s choices sheet also
exists as a defined list in \code{dev}'s choices sheet. Returns a tibble row for
each list name present in \code{target}'s choices but absent from \code{dev}'s choices.
}
\details{
\subsection{Relationship to other checks}{

This check is a prerequisite for \code{\link[=validate_choices]{validate_choices()}}: because
\code{\link[=validate_choices]{validate_choices()}} only compares options for lists that exist in \emph{both}
forms' choices sheets, any list that \code{target} defines but \code{dev} omits would
be silently skipped. \code{validate_list_names()} catches those gaps explicitly.

To verify that the same lists are also actively \emph{used} in both forms' survey
questions (not just defined in choices), see \code{\link[=validate_survey_list_names]{validate_survey_list_names()}}.
}
}
\examples{
target <- read_xlsform(system.file("extdata/form.xlsx", package = "Idem"))

# No issues: all lists defined in target's choices are also defined in dev
validate_list_names(target, target)

# Issues found: dev has no choice lists at all, but target defines some
dev_empty_choices <- xlsform(
  survey  = target$survey,
  choices = data.frame(list_name = character(), name = character())
)
validate_list_names(target, dev_empty_choices)
}
\seealso{
\code{\link[=validate_xlsform]{validate_xlsform()}} to run all checks together;
\code{\link[=validate_survey_list_names]{validate_survey_list_names()}} for the complementary survey-side check;
\code{\link[=xlsform_choices_list_names]{xlsform_choices_list_names()}} to extract defined list names from a form.
}
