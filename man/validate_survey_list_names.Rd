% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/validate_survey_list_names.R
\name{validate_survey_list_names}
\alias{validate_survey_list_names}
\title{Validate survey-referenced list names between two XLSForms}
\usage{
validate_survey_list_names(target, dev)
}
\arguments{
\item{target}{An \code{xlsform} object representing the authoritative reference
form.}

\item{dev}{An \code{xlsform} object representing the form being validated.}
}
\value{
A tibble with columns \code{check}, \code{severity}, \code{name}, \code{list_name}, and
\code{detail}. Has zero rows when all list names referenced by \code{target}'s survey
are also referenced by \code{dev}'s survey.
}
\description{
Checks that every list name \emph{referenced} in \code{target}'s survey questions is
also referenced in \code{dev}'s survey questions. Returns a tibble row for each
list name actively used by \code{target}'s survey that is absent from \code{dev}'s
survey.
}
\details{
\subsection{How it differs from \code{validate_list_names()}}{

\code{\link[=validate_list_names]{validate_list_names()}} compares the lists \emph{defined} in each form's choices
sheet. \code{validate_survey_list_names()} compares the lists actively \emph{used} by
survey questions — the second token in \code{type} values like
\verb{select_one list_a}.

The two checks are complementary. A list can be defined in choices but
never used in the survey (orphaned list), or — after a question type change
from \verb{select_one list_a} to \code{text} — it may still be defined in choices
while no longer referenced in any survey question. This check surfaces the
latter case.
}
}
\examples{
target <- read_xlsform(system.file("extdata/form.xlsx", package = "idem"))

# No issues: all lists target's survey uses are also used in dev's survey
validate_survey_list_names(target, target)

# Issues found: dev's survey has all select questions replaced by text,
# so none of target's referenced lists appear in dev's survey
dev_no_selects <- xlsform(
  survey = data.frame(
    type = rep("text", nrow(target$survey)),
    name = target$survey$name
  ),
  choices = target$choices
)
validate_survey_list_names(target, dev_no_selects)
}
\seealso{
\code{\link[=validate_xlsform]{validate_xlsform()}} to run all checks together;
\code{\link[=validate_list_names]{validate_list_names()}} for the complementary choices-side check;
\code{\link[=xlsform_referenced_list_names]{xlsform_referenced_list_names()}} to extract referenced list names from a form.
}
