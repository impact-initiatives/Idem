% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/xlsform_translations.R
\name{is_malformed_translation_col}
\alias{is_malformed_translation_col}
\title{Test whether a string is a malformed translated field column name}
\usage{
is_malformed_translation_col(x)
}
\arguments{
\item{x}{A character vector of column names to test.}
}
\value{
A logical vector the same length as \code{x}. \code{TRUE} where \code{x} looks
like a malformed translation column, \code{FALSE} otherwise.
}
\description{
Checks whether a character string looks like a translation attempt for a
\link{translatable_fields} prefix but does not conform to the valid XLSForm
convention (as detected by \code{\link[=is_translated_col]{is_translated_col()}}). A column is considered a
malformed translation attempt when, after trimming whitespace, it either
exactly equals a translatable field (bare, e.g. \code{"label"}) or starts with
a translatable field followed immediately by a colon (single or double,
e.g. \code{"label:Kreyol"}, \code{"label::English"}, \code{"label::French (FR)"}).
}
\details{
Columns that use other separators (e.g. \code{"label_analysis_var"},
\code{"label.question"}) are \strong{not} considered translation attempts and return
\code{FALSE}.

The function is vectorised over \code{x}.
}
\examples{
is_malformed_translation_col(
  c("label", "label::English", "label::English (en)")
)
}
\seealso{
\link{translatable_fields}, \code{\link[=is_translated_col]{is_translated_col()}}
}
